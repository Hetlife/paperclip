import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-geist-sans",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Atelier Fauna — Living Biotopes for the Considered Home",
  description:
    "A luxury, interactive introduction to captive-bred fauna as living biotopes — paired with the Sanctuary Journal, a self-help curriculum drawn from the natural world.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
